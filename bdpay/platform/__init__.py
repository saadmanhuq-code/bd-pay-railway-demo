"""bdpay.platform — primitives and cross-package contracts (dependency root).

Canonical JSON (errata E12), content-addressed IDs, integer-paisa Money,
injectable Clock, Bangladesh PII redaction, the binding error envelope,
environment Settings, and Bengali numeric normalization. Every other bdpay
package imports these; this package imports nothing outside the stdlib.
"""
