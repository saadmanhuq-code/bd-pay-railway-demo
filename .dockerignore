# Gateway Dockerfile.gateway only COPY-selects pyproject/bdpay/db/docker,
# so FE trees in context are fine (and REQUIRED for Railpack FE builds).

.git
.venv
**/__pycache__
*.pyc
.pytest_*
*.egg-info

# Heavy non-runtime trees
arch/
docs/
research/
spec/
monitoring/

# Dev-only stack
docker-compose.dev.yml
docker-compose.prod.yml
uv.lock
